diff --git a/src/demux/transmuxer.ts b/src/demux/transmuxer.ts
index d7e0d3085..df7394718 100644
--- a/src/demux/transmuxer.ts
+++ b/src/demux/transmuxer.ts
@@ -169,6 +169,41 @@ export default class Transmuxer {
       }
     }
 
+
+    // Tsundoku download mode taps complete plaintext segments while preserving the stock
+    // demux/remux path that advances hls.js's stream-controller state.
+    if (
+      !keyData &&
+      (
+        this.config as HlsConfig & {
+          tsundokuCaptureFragments?: boolean;
+        }
+      ).tsundokuCaptureFragments
+    ) {
+      const offset =
+        this.demuxer instanceof TSDemuxer
+          ? Math.max(0, TSDemuxer.syncOffset(uintData))
+          : 0;
+      const end =
+        this.demuxer instanceof TSDemuxer
+          ? uintData.length - ((uintData.length - offset) % 188)
+          : uintData.length;
+      const payload = uintData.subarray(offset, end);
+      this.observer.emit(Events.FRAG_DECRYPTED, Events.FRAG_DECRYPTED, {
+        // TransmuxerInterface fills this before forwarding the event.
+        frag: undefined as any,
+        payload:
+          payload.byteOffset === 0 &&
+          payload.byteLength === payload.buffer.byteLength
+            ? payload.buffer
+            : payload.slice().buffer,
+        stats: {
+          tstart: stats.executeStart,
+          tdecrypt: now(),
+        },
+      });
+    }
+
     if (discontinuity || trackSwitch || initSegmentChange || resetMuxers) {
       this.resetInitSegment(
         initSegmentData,
diff --git a/src/remux/mp4-remuxer.ts b/src/remux/mp4-remuxer.ts
index 96b0e59b8..c21f40bf9 100644
--- a/src/remux/mp4-remuxer.ts
+++ b/src/remux/mp4-remuxer.ts
@@ -460,6 +460,23 @@ export default class MP4Remuxer extends Logger implements Remuxer {
           height: videoTrack.height,
         },
       };
+      // A download writes one file, so it needs one moov covering both tracks; the segments above
+      // are the per-SourceBuffer init segments MSE wants. The field hangs off tracks.video rather
+      // than off tracks itself because BufferController and StreamController both iterate
+      // Object.keys(tracks) and would treat a sibling key as a SourceBuffer name. An 'audio/mpeg'
+      // container is mp3 passed through as raw MPEG with an empty moof, so it gets no combined init
+      // and the download falls back to the plaintext transport stream.
+      if (
+        (this.config as HlsConfig & { tsundokuCaptureFragments?: boolean })
+          .tsundokuCaptureFragments &&
+        tracks.audio &&
+        tracks.audio.container !== 'audio/mpeg'
+      ) {
+        (tracks.video as any).tsundokuInitSegment = MP4.initSegment([
+          videoTrack,
+          audioTrack,
+        ]);
+      }
       if (computePTSDTS) {
         trackId = videoTrack.id;
         timescale = videoTrack.inputTimeScale;
